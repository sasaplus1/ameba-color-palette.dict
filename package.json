{
  "devDependencies": {
    "ameba-color-palette.css": "^4.6.0",
    "css-tree": "^1.1.3"
  },
  "private": true,
  "scripts": {
    "attach": "git worktree add ./dist master",
    "build": "node ./npm_scripts/extract.js > ./dict/ameba-color-palette.dict",
    "detach": "git worktree remove ./dist",
    "release": "cp -rf ./dict ./dist"
  }
}
